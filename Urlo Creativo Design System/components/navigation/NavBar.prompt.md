The floating pill nav that tops every page — logo mark left, centred links, `IT` language toggle right. The active item is bold with a 1.5px underline.

```jsx
<NavBar active="Services" basePath="../../"
  onNavigate={(label) => goTo(label)} />
```

Sits over paper with a faint inset hairline and light blur. Keep it pinned near the top with side margins.
