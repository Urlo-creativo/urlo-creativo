A small, quiet pill for meta labels, categories and filters.

```jsx
<Tag>2026</Tag>
<Tag variant="fill">Art direction</Tag>
<Tag variant="muted">Brand identity</Tag>
```

Variants: `outline` (default), `fill` (yellow), `muted` (gray ring). Use wide tracking, sentence/caps as the context dictates.
