The "HOW WE WORK" stage bar — caps labels over a band of process-accent colours.

```jsx
<ProcessBar />  {/* Discovery → Strategy → Identity → Development → Production → Growth */}
```

Pass `stages={[{label, color}]}` to customise. Colours come from the `--stage-*` tokens. Pairs with a `SectionTitle` reading "HOW WE WORK".
