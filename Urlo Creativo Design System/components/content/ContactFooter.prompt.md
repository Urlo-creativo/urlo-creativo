The yellow panel that closes every page — huge CONTACT headline, address/contacts column, text social links, and an outline "Book a consultation" pill.

```jsx
<ContactFooter />
<ContactFooter email="hello@urlocreativo.com" cta="Get in touch" />
```

Always full-bleed on `--uc-yellow`. Socials are plain text links (no glyphs).
